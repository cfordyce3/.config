# Custom dotfiles
Here you'll find a collection of my config files.

### Notes

**Any new config must be manually removed (!) from the blanket .gitignore**

#### zsh
* zsh requires a symlink `ln -s ~/.config/zsh/.zshrc ~/.zshrc`


